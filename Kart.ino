#include "common.h"
#include "motor_control.h"
#include "heat_control.h"
#include "communication.h"


void setup() {
    //motor_control_init(1.0f);
    //heat_control_init();

    //xTaskCreate(speed_command_task, "Speed command task", 1000, NULL, 1, NULL);
    //xTaskCreate(heat_control_task, "Heat control task", 1000, NULL, 1, NULL);
    //xTaskCreate(motor_control_task, "Motor control task", 1000, NULL, 1, NULL); 
    //xTaskCreate(communication_task, "Communication task", 1000, NULL, 1, NULL);
    return;
}

void loop() {
  return;
}